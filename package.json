{
  "name": "csv-to-json",
  "version": "1.0.0",
  "description": "Commandline application that takes in CSV as input and converts it to JSON",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "npx ts-node ./src/index.ts"
  },
  "keywords": [
    "csv",
    "json"
  ],
  "author": "Edvinas",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^22.9.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.6.3"
  },
  "dependencies": {
    "better-sqlite3": "^11.5.0"
  }
}
